import { styled } from "@mui/material/styles";

export const Profile = styled('div')(({  }) => ({
    display: 'flex',
    gap: '16px',
    alignItems: 'center',
    padding: '4px',
    justifyContent: 'space-between',
}));