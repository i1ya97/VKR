name: Build and Publish image to Docker Hub
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  # publish_image: 
  #   runs-on: ubuntu-latest
  #   - uses: actions/checkout@v4
  #   - name: Build the Docker image
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build ./web/ --file Dockerfile --tag my-image-name:$(date +%s)
